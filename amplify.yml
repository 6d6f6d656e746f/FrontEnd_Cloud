version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build - deploying static files from public"
  artifacts:
    # Amplify will publish the files located in public
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
